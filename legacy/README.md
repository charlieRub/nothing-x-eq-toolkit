# Legacy files

This folder contains earlier one-off generators and QR outputs kept for traceability.

The public workflow should use:

```text
src/
scripts/
presets/
qr/
docs/
skills/
```

Do not use legacy QR files as the recommended import source. Use the clean generated PNG files in `qr/`.
